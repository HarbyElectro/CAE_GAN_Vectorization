experiment:
  name: gan_ae_image_folder
  description: GAN-guided Autoencoder trained on a generic folder-based image dataset.
  output_dir: runs/gan_ae_image_folder
  seed: 42

dataset:
  name: GenericImageFolder
  type: image_folder
  data_dir: data/your_dataset
  image_size: 64
  channels: 3

model:
  name: gan_autoencoder
  framework: pytorch
  base_channels: 64
  latent_channels: 16

training:
  epochs: 50
  batch_size: 128
  optimizer: adam
  learning_rate: 0.0002
  reconstruction_loss_weight: 0.6
  checkpoint_every: 1

evaluation:
  eval_batch: 16
  eval_n: 5
  discriminator_threshold: 0.5
  metrics:
    - ssim
    - ms_ssim
    - psnr
    - cosine_similarity
    - euclidean_distance
    - manhattan_distance

outputs:
  save_checkpoints: true
  checkpoint_dir: runs/gan_ae_image_folder
  save_latents: true
  latents_path: runs/gan_ae_image_folder/latents.h5
  latents_n: 5000
  latents_dtype: float16
  save_model_config: true
  save_weights_json: false
  store_images_in_h5: false
  h5_gzip: 4
